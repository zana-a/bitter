spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/bitter
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
